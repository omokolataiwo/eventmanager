import database from '../models';
import Event from './_support/event';

module.exports = {
  createEvent(req, res) {
    
  },
  deleteEvent(req, res) {
   
  },
  editEvent(req, res) {
    
  },
};
